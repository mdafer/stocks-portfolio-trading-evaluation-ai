services:
  backend:
    build: ./backend
    env_file: ./backend/.env
    volumes:
      - backend-data:/app/data
    ports:
      - "3011:3000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3010:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  backend-data:
